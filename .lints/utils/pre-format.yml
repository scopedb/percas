# Replace `@` with `-` in macros
id: pre-format-0
language: Rust
rule:
  pattern: '@'
  precedes:
    any:
      - kind: string_literal
      - kind: raw_string_literal
  inside:
    kind: macro_invocation
    stopBy: end
fix: '-'
